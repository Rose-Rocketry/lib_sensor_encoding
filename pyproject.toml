[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "lib-sensor-encoding"
version = "0.0.1"
dependencies = [
  "construct",
]

[tool.setuptools.packages]
find = {}
