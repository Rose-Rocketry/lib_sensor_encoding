[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "lib-sensor-encoding"
version = "0.1.0"
dependencies = [
  "msgpack",
  "paho-mqtt",
]

[tool.setuptools.packages]
find = {}
